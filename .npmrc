# Disable optional dependencies to prevent native module compilation
ignore-optional=true

# Disable scripts during installation
ignore-scripts=true

# Force using npm instead of other package managers
engine-strict=true

# Disable audit to speed up installation
audit=false

# Disable fund messages
fund=false

# Use legacy peer deps to avoid dependency conflicts
legacy-peer-deps=true

